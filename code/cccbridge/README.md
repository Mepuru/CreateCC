# code/cccbridge/ — 依赖 CC:C Bridge 的项目

这里放用到 CC:C Bridge 外设的程序：`create_source`、`create_target`、`redrouter`、`scroller`、`animatronic`。

- 类型名与方法表：`docs/API_CCCBridge.md`
- 该 mod 自带文档：`docs/cccbridge/docs/peripherals/*.md`（**注意示例代码有笔误，以函数表为准**）
- 前提：用户确认装了 CC:C Bridge 1.7.3（NeoForge，1.21.1）
